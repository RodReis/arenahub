{
  "name": "@arenahub/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Design system do ArenaHub: pipeline de tokens e primitivas compartilhadas",
  "//exports": "O `dist` e o entrypoint, e o build COPIA os `.module.css` para la. O `tsc` sozinho nao copia: ele transpila o `.tsx` e deixa `import './Button.module.css'` apontando para um arquivo que nunca chegou. Apontar o entrypoint para `src` nao resolve -- os imports internos usam extensao `.js` (NodeNext), e o bundler lendo `.tsx` nao mapeia `.js` de volta.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./src/index.ts"
    },
    "./theme.css": "./dist-tokens/theme.css",
    "./tokens": {
      "types": "./dist/tokens.generated.d.ts",
      "import": "./dist/tokens.generated.js",
      "default": "./src/tokens.generated.ts"
    },
    "//app-tokens": "Entrypoint PROPRIO do app (apps/mobile), separado de `.` de proposito: o entrypoint principal exporta componente React web e arrastaria react-dom e recharts para o bundle do celular. Este aqui e so objeto literal -- nenhuma dependencia, nada que o Metro precise resolver.",
    "./app-tokens": {
      "types": "./dist/app-tokens.generated.d.ts",
      "import": "./dist/app-tokens.generated.js",
      "default": "./src/app-tokens.generated.ts"
    }
  },
  "files": [
    "dist",
    "dist-tokens",
    "src",
    "tokens"
  ],
  "scripts": {
    "build:tokens": "node scripts/build-tokens.mjs",
    "build": "node scripts/build-tokens.mjs && tsc -p tsconfig.build.json && node scripts/copy-css.mjs",
    "//lint": "Escopo em `src` porque `scripts/build-tokens.mjs` e JavaScript de build que roda em Node, fora do programa TypeScript do pacote -- o parser type-aware exigiria inclui-lo no tsconfig, e incluir arrastaria o pipeline para o typecheck do que ele mesmo gera. Mesmo tratamento dos `scripts/*.mjs` da raiz, que tambem nao sao lintados.",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@arenahub/config": "workspace:*",
    "@testing-library/jest-dom": "6.9.1",
    "@testing-library/react": "16.3.0",
    "@testing-library/user-event": "14.6.1",
    "@types/node": "26.2.0",
    "@types/react": "19.2.14",
    "@types/react-dom": "19.2.4",
    "@vitest/coverage-v8": "4.1.10",
    "eslint": "9.39.5",
    "jsdom": "27.0.0",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  },
  "engines": {
    "node": ">=22.5.0 <23.0.0"
  },
  "dependencies": {
    "@arenahub/access-policy": "workspace:*",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "recharts": "3.10.1"
  }
}
